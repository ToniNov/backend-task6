
export type MessageType = {
    _id: string;
    from: string;
    to: string;
    title: string;
    message: string;
    createdAt: string;
}

export type UserType = {
    _id: string;
    name: string;
}